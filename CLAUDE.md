@AGENTS.md

# Claude Code

`AGENTS.md` is the single maintained repository instruction source. Read and follow it in full; do not duplicate or fork its standing rules here.

Use Claude-specific capabilities (Plan Mode, skills, plugins, subagents, hooks, MCP) only when they materially help the current task and remain consistent with `AGENTS.md`.

If a direct current instruction from Mihai conflicts with `AGENTS.md`, follow the direct instruction and surface the conflict explicitly.
